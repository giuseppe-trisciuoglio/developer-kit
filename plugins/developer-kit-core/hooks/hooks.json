{
  "description": "Core hooks: prevent destructive commands; stop hooks temporarily disabled",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/prevent-destructive-commands.py",
            "statusMessage": "Checking command safety..."
          }
        ]
      }
    ],
    "Stop": []
  }
}
